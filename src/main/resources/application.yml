spring:
  datasource:
    url: jdbc:mysql://134.185.119.223:65103/test
    username: root
    password: swlug2024
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  data:
    mongodb:
      uri: mongodb://root:root@158.179.163.126:65525/swlug?authSource=admin

server:
  servlet:
    session:
      timeout: 3m

servlet:
  multipart:
    enabled: true
    max-file-size: 10MB
    max-request-size: 20MB

